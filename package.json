{
  "name": "@lokua/midi-util",
  "version": "1.0.0-beta.1",
  "description": "Constants for MIDI status types and some basic helper functions (mtof, ftom)",
  "main": "dist/index.js",
  "scripts": {
    "test": "node src/index.spec",
    "build": "babel src/index.js -o dist/index.js",
    "clean": "rm -rvf dist && mkdir dist",
    "prepublishOnly": "npm run clean && npm run test && npm run build",
    "lint": "eslint src"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/lokua/midi-util.git"
  },
  "author": "Joshua Kleckner <lokua@lokua.net>",
  "keywords": [
    "midi",
    "util"
  ],
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "6.26.0",
    "babel-preset-env": "1.7.0",
    "eslint": "^4.3.0",
    "eslint-config-prettier": "3.3.0",
    "eslint-plugin-prettier": "3.0.1",
    "prettier": "1.15.3"
  },
  "dependencies": {}
}
